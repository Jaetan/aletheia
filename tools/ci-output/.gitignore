# Run records kept locally and never tracked: the logs tools/run_ci.py writes,
# the falsifiable evidence behind a "gates green" claim in a commit message,
# and under probes/ the output of every probe that failed the latest run of
# probes/run_all.sh. Dev-machine artifacts, not a source of truth.
*
!.gitignore
