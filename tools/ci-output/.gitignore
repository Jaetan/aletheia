# CI run logs from tools/run-ci.sh.  Kept locally as falsifiable evidence
# behind "all gates clean" / "gates green" assertions in commit messages,
# but never tracked in git — logs are dev-machine artifacts, not source of
# truth.
*
!.gitignore
