# Sanitizer ignorelist for the C++ binding.
#
# Format: one entry per line, see
# https://clang.llvm.org/docs/SanitizerSpecialCaseList.html.  This file is
# passed as -fsanitize-ignorelist= to all sanitizer-instrumented builds
# (-DALETHEIA_SANITIZER=...).  Aletheia's own code remains fully
# instrumented; only specific third-party vendored sources fall under the
# ignorelist.
#
# Each entry below states what it suppresses and why.

# XLStyles.cpp loads a bool from spreadsheet bytes that can hold values
# other than 0 and 1, which is UB per the C++ standard.  Measured by building
# the Excel tests under UBSan with this list withheld and recovery enabled
# (the measurement script lives with the review record): two reports, both
# from that file, and none from anywhere else in the fetched tree.  Aletheia
# uses OpenXLSX read-only through the optional excel loader and cannot patch
# it without forking, so its sources are suppressed as a whole; aletheia-cpp's
# own code remains fully instrumented.  The zip and XML implementations
# OpenXLSX fetches for itself are outside this pattern and report nothing.
src:*OpenXLSX/*
