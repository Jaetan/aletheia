# CLAUDE.md

Guidance for Claude Code (claude.ai/code) when working in this repository.

## Project Overview

Aletheia is a formally verified CAN frame analysis system using Linear Temporal Logic (LTL). Core logic in Agda with correctness proofs, compiled to Haskell, exposed through Python, C++, and Go APIs. Phase status: [PROJECT_STATUS.md](PROJECT_STATUS.md). Mission: [docs/PITCH.md](docs/PITCH.md).

## Development Environment

**Must be preserved across session compression.**

- Agda binary: `/home/nicolas/.cabal/bin/agda`
- Shell: `/usr/bin/fish` (config at `/home/nicolas/.config/fish/config.fish`)
- User binaries: `/home/nicolas/.local/bin`; libraries: `/home/nicolas/.local/lib`

**Type-check command** (always cap heap):
```bash
/home/nicolas/.cabal/bin/agda +RTS -N32 -M16G -RTS src/Aletheia/YourModule.agda
```
- `-N32`: parallel GHC; critical for Protocol/StreamState.agda and Main.agda (17s vs >120s timeout).
- `-M16G`: heap cap; prevents runaway elaboration on the 62 GiB host. Doubles as a tripwire — bump only when a specific module legitimately needs it.
- First build compiles stdlib (~20s, cached thereafter).

## Global Project Rules

### AGENTS.md as Coding Standards

[AGENTS.md](AGENTS.md) defines per-language categories, guidelines, and verification commands. **Follow these as coding standards when writing code, not only as review checklists.** Consult the relevant language section before writing/modifying code.

### User Shorthands

When the user's message is just `UPD` (case-insensitive, no other content), interpret it as **"Update session state, memory/feedback, plan/project status, CLAUDE.md/AGENTS.md."** Sweep:
- `.session-state.md` (gitignored — local resume notes)
- `MEMORY.md` + relevant files under `memory/` (open-work pointers; new feedback memories if a generalizable lesson surfaced)
- `PROJECT_STATUS.md` and `docs/development/PARITY_PLAN.md` (the two roadmap surfaces — keep in sync)
- `CLAUDE.md` (Current Session Progress, module-flag breakdown, anything that drifted)
- `AGENTS.md` (only if a new rule / cross-ref was earned this session)

**UPD is a doc-state sync only.** The resulting commit must contain ONLY doc-sweep edits. Pre-existing uncommitted work (refactors, structural cleanups, prior tasks) goes in its own commit at task completion, never bundled into UPD. See `memory/feedback_upd_scope.md`. Apply the 2-question pre-commit gate (`feedback_pre_commit_scope_check.md`) before committing the doc sweep.

When the user's message is just `READ` (case-insensitive, no other content), interpret it as **"Read the session state, memory/feedback, plan/project status, CLAUDE.md/AGENTS.md."** Sweep (read-only — no edits):
- `.session-state.md` (gitignored — local resume notes)
- `MEMORY.md` + relevant files under `memory/` (open-work pointers, feedback memories)
- `PROJECT_STATUS.md` and `docs/development/PARITY_PLAN.md` (the two roadmap surfaces)
- `CLAUDE.md` (already loaded into context)
- `AGENTS.md` (per-language coding standards)

READ is the read-only counterpart of UPD: rehydrate context at session start, do not write.

### Agda Module Requirements (MANDATORY)

Every Agda module MUST start with:
```agda
{-# OPTIONS --safe --without-K #-}
```

- `--safe`: no postulates, no unsafe primitives, no non-terminating recursion.
- `--without-K`: HoTT compatibility (no Streicher's K).
- Library-level `--erasure` (in `aletheia.agda-lib`) enables `@0` for zero-cost phantom parameters (e.g. `Timestamp μs`).

**Exceptions**: postulates require a separate `*.Unsafe.agda` module (drop `--safe` only there); allowlisted by name in `Shakefile.hs`. `cabal run shake -- check-invariants` rejects any other `^postulate` line or `Unsafe`-named module, and CI runs `check-invariants` on every build.

### Module Safety Flag Breakdown

243 total modules (`cabal run shake -- count-modules`):
- **238**: `--safe --without-K`
- **4**: `--safe --without-K --no-main` (Main.agda, Main/JSON.agda, Main/Binary.agda, Parser/Combinators.agda)
- **1**: `--without-K` only — `Aletheia/DBC/TextParser/Properties/Substrate/Unsafe.agda`, the allowlisted Unsafe substrate hosting the two `String ↔ List Char` bridging axioms (`toList∘fromList`, `fromList∘toList`) AND the B.3.d outer-wrap `parseText-on-formatText` consumer (post-Layer 4c task E, 2026-05-03) — co-located in this single module to keep the trusted-axiom-consuming surface at one allowlisted module by name (mirrors stdlib's `Data.String.Unsafe`; structurally unprovable in `--safe --without-K` because Agda's String primitives reduce only on closed terms).

242 of 243 modules use `--safe`. No modules require `--sized-types`. Path A.4 (3d.4 + JSON-mirror, 2026-04-27) lifted the prior 47-module `--without-K`-only cluster to `--safe --without-K` by retyping `Identifier.name`, JSON `JString`, DBC AST text fields, and LTL signal names from `String` to `List Char`. Phase E.5β (2026-05-06) added 3 new `--safe --without-K` modules — `Format/ValueDescription`, `Properties/ValueTables/ValueDesc`, `Properties/Aggregator/Dispatcher/Simple/ValueDesc` — for VAL_ promotion.  Phase E.6 (2026-05-07) added 2 new `--safe --without-K` modules — `TextParser/ValueDescriptions` (runtime: `attachValueDescs` + `collectFromMessages`) and `Properties/Aggregator/Refine/ValueDescriptions` (proof: inverse-bridge through `lookup-on-collected`). Phase E.7 (2026-05-07) added 0 new modules; only edits to `TextFormatter/{ValueTables,TopLevel}`, `Foundations`, `Partition`, `BodyBridge`, `Universal`, and `Refine/ValueDescriptions` (latter gained `map-attachToMessage-on-collected` + `collectFromMessages-vds-empty` helpers). Phase E.8 (2026-05-08) added 1 new `--safe --without-K` module — `Formatter/WellFormedText/ValueDescResolves` (predicate: `ResolvesValueDesc rvd d` Σ-witness for E.11 CHECK 23 + `WellFormedTextDBC.vds-resolve` field).  Phase E.9a (2026-05-08) added 0 new modules; lifted E.1's interim `vds-empty`/`vds-eq` clauses on `MessageWF`/`WellFormedTextSignal` and replaced E.7's vacuous `tvd-WF` arm with non-vacuous derivation from signal-name validity via a `clearVds`/`clearVdsMsg` structural-transform cascade through `liftTopStmt` (the single proof-only fork point); ~9 files modified, ~300 LOC end-to-end.  Per-commit module-count drift since 3d.5 is recorded in PROJECT_STATUS.md and `memory/project_b3d_universal_proof.md`.

## Common Commands

See [Building Guide](docs/development/BUILDING.md). Quick reference:

```bash
# Type-check a single module
cd src && agda +RTS -N32 -M16G -RTS Aletheia/YourModule.agda

# Build everything (Agda → Haskell → libaletheia-ffi.so)
cabal run shake -- build

# Tests (each from the right cwd)
cd python && python3 -m pytest tests/ -v
cd python && basedpyright aletheia/
cd python && pylint aletheia/
cd cpp && cmake -B build && cmake --build build && ctest --test-dir build
cd go && go test ./aletheia/ -v -count=1 -race

# Cross-language benchmarks
bash benchmarks/run_all.sh --frames 1000 --runs 5 --bench throughput
```

## Architecture

Three-layer design: [docs/architecture/DESIGN.md](docs/architecture/DESIGN.md).

Agda packages: **Parser/**, **CAN/**, **DBC/**, **LTL/** (Syntax, Incremental, Semantics, Adequacy, Coalgebra, SignalPredicate, SimplifySound, Reachable, JSON), **Trace/**, **Protocol/**. Full file tree: [README.md](README.md#project-structure).

## Development Workflow

1. Edit Agda source.
2. Type-check fast: `cd src && agda +RTS -N32 -M16G -RTS Aletheia/Parser/Combinators.agda`.
3. Full build: `cabal run shake -- build` (also rebuilds `libaletheia-ffi.so`).
4. Run tests for affected bindings.

Shake tracks Agda dependencies. First full build ~60s; subsequent ~5–15s.

## Key Files

- **aletheia.agda-lib**: Agda library config (pinned stdlib version)
- **Shakefile.hs**: build orchestration (Agda → Haskell → shared library)
- **haskell-shim/aletheia.cabal**: Haskell package + `foreign-library aletheia-ffi`
- **haskell-shim/src/AletheiaFFI.hs**: FFI exports (Python ctypes, C++/Go dlopen)
- **python/pyproject.toml**: Python package config
- **cpp/CMakeLists.txt**: C++23 build (CMake 3.25+, FetchContent for nlohmann/json + Catch2)
- **docs/FEATURE_MATRIX.yaml**: cross-binding feature parity matrix; structural gate tests in `python/tests/`, `go/aletheia/`, `cpp/tests/` fail CI on silent symbol removal. Roadmap: [docs/development/PARITY_PLAN.md](docs/development/PARITY_PLAN.md).

## Important Notes

### Agda Compilation

- `--safe --without-K` mandatory (header pragma + `check-invariants`); the lone `--without-K`-only exception (`Substrate.Unsafe`) is documented in the flag breakdown.
- Generated MAlonzo lives in `build/`; never edit — modify Agda source.

### MAlonzo FFI Name Mangling

MAlonzo mangles names (e.g., `processJSONLine` → `d_processJSONLine_4`). Build auto-detects mismatches and prints exact `sed` fix commands — just run them. Triggers rarely (only when adding/removing definitions before `processJSONLine` in Main.agda). Keep `AletheiaFFI.hs` minimal; alternatives like COMPILE pragmas would compromise `--safe`.

### Haskell FFI Layer

3 files (~470 LOC, no business logic):
- **AletheiaFFI.hs** (~277 LOC): `foreign export ccall` wrappers around `processJSONLine` (JSON commands) and `processFrameDirect` (binary frames via `aletheia_send_frame`).
- **AletheiaFFI/Marshal.hs** (~95 LOC): Agda type construction helpers.
- **AletheiaFFI/BinaryOutput.hs** (~99 LOC): binary response encoding.

State managed via `StablePtr (IORef StreamState)`. All bindings load `.so` via ctypes/dlopen — no subprocess overhead.

### C++ Binding (`cpp/`)

Wraps `libaletheia-ffi.so` via `dlopen`. `IBackend` interface; `MockBackend` for tests. Strong types (`std::byte`, validated newtypes, `std::expected`). Custom `Logger` (~90L, callback-based, 15 event types matching Go's slog, zero-cost when null). RTS cores via `make_ffi_backend(path, rts_cores)` (default 1, once-per-process with mismatch warning). C++23, targets g++>=14 / clang>=21. Style: `.clang-format` + `.clang-tidy`. Inventory: [PROJECT_STATUS.md § Key Metrics](PROJECT_STATUS.md#key-metrics).

### Go Binding (`go/`)

Wraps `libaletheia-ffi.so` via cgo + dlopen. `Backend` / `MockBackend` / `FFIBackend` (with C trampolines). Strong types (`[]byte` payload + DLC validation, validated CAN ID / DLC newtypes, sealed CanID/Predicate/Formula interfaces). `slog` via `WithLogger` option (15 event types); `ViolationEnrichment.CoreReason` carries Agda core reason strings. RTS cores via `WithRTSCores` functional option. `Client` is goroutine-safe (`sync.Mutex`), double-close safe, GHC RTS init thread-pinned (`LockOSThread`). Optional `go/excel/` is a separate Go module pulling `xuri/excelize`; depend on it only for the Excel loader. Inventory: [PROJECT_STATUS.md § Key Metrics](PROJECT_STATUS.md#key-metrics).

### Module Organization

Follow existing package structure (Parser, CAN, DBC, LTL, …). Include correctness properties alongside implementations (`Properties.agda`). Update Main.agda if new functionality needs FFI exposure.

### Import Naming Conventions

When stdlib operators clash, use **subscript suffix** for consistency:
- String: `_++ₛ_`, `_≟ₛ_`
- List: `_++ₗ_`
- Rational: `_+ᵣ_`, `_*ᵣ_`, `_-ᵣ_`, `_≤ᵣ_`

```agda
open import Data.String using (String) renaming (_++_ to _++ₛ_)
open import Data.List using (List) renaming (_++_ to _++ₗ_)
open import Data.Rational using () renaming (_+_ to _+ᵣ_; _*_ to _*ᵣ_)

result   = "hello" ++ₛ "world"
combined = list1 ++ₗ list2
```

Underscores are invisible in infix usage but remain when passing operators as parameters (e.g., `foldr _++ₛ_ ""`).

## Troubleshooting

Build-time issues are catalogued in [BUILDING.md § Troubleshooting](docs/development/BUILDING.md#troubleshooting). Common ones:

- **Build failures**: `cabal run shake -- clean && cabal run shake -- build`.
- **MAlonzo name mismatch**: build prints exact `sed` command — run it.
- **Type-checking timeout**: always `+RTS -N32 -M16G -RTS`.
- **`hs_init` failure / `aletheia_init() returned null`**: `.so` built against different GHC than loaded. Rebuild (`cabal run shake -- build`); ensure no stale copy in `$LD_LIBRARY_PATH`.
- **`.so` load failure**: loader checks `_install_config.LIBRARY_PATH` → `LD_LIBRARY_PATH` → `/usr/local/lib`. Regen via `cabal run shake -- install` or set `ALETHEIA_FFI_PATH`.
- **ctypes signature mismatch (Python)**: `.so` and Python package versions drifted. Compare `python -m aletheia --version` vs `strings libaletheia-ffi.so | grep aletheia-ffi-`.
- **DBC validation rejection**: check `ValidationIssue.code` enum — table in [PROTOCOL.md § Common Error Codes](docs/architecture/PROTOCOL.md#common-error-codes). `aletheia validate --dbc <file>` to see all issues.
- **Property formula parse error**: JSON schema is strict (`"operator"` lowercase, predicates under `{"operator": "atomic", "predicate": {...}}`). Compare against `Signal("X").equals(1).to_dict()` output.

## Performance Considerations

- **Parser combinators**: structural recursion on input length, not fuel — fuel breaks termination or blows up type-checking. See `Parser/Combinators.agda`.
- **Type-checking**: always `+RTS -N32 -RTS` (StreamState/Main otherwise time out past 120s).
- **Hot path**: `Dec`-valued predicates allocate proof terms per call in MAlonzo. Replace with `Bool`-valued fast path + equivalence lemma. See `extractSignalCoreFast` for the pattern.

## Implementation Phases

[PROJECT_STATUS.md](PROJECT_STATUS.md). Current state: Phase 5.1 complete (binary FFI 4.3× CAN 2.0B / 9.1× CAN-FD; CAN-FD; C++/Go bindings; cross-language benchmarks; four-tier check interface with full parity). Active track: Phase E (VAL_ promotion to `DBCSignal.valueDescriptions`).

---

## Notes for newcomers

Start with the [Project Pitch](docs/PITCH.md) for context.

**Operational pitfalls** (most are caught by build/lint, but easy to trip on first time):
- `Dec`-valued predicates on the streaming hot path: MAlonzo allocates per call. Use `Bool`-valued fast path + equivalence lemma (`extractSignalCoreFast`).
- Fuel-based parser combinators: structural recursion on `length input` only.
- Type-checking without `+RTS -N32 -RTS`: large modules time out past 120s.
- Running tools from the repo root: `pytest` / `basedpyright` / `pylint` need `cd python` first (config picks up nearest `pyproject.toml`).

**Key terms used elsewhere in this file:**
- **MAlonzo**: Agda's Haskell backend. `agda --compile` produces a `MAlonzo/` directory of generated `.hs` files; the Cabal package and FFI shared library are built on top. Function names get mangled.
- **`Dec A`**: A type expressing decidability (`yes (a : A) ⊎ no (¬ A)`). Carries a *proof object* at runtime — that's why it allocates on hot paths.

**Code style**: per-language conventions live in [AGENTS.md](AGENTS.md). Don't duplicate here.

**Pre-commit minimum** (doc-only changes): `agda +RTS -N32 -RTS src/Aletheia/Main.agda` → `cabal run shake -- build` → relevant binding tests.

**For code changes**, the Agda-side minimum is `build` PLUS the proof-side Shake gates — `build` only type-checks Main.agda's runtime transitive closure (the runtime path that flows into `libaletheia-ffi.so`), so Properties / *Roundtrip / *WF / Substrate.Unsafe modules are NOT reached by it. Run all of:
- `cabal run shake -- check-properties` — walks the proof tree (Properties / *Roundtrip / *WF + universal aggregator + Substrate.Unsafe); the actual proof-correctness gate
- `cabal run shake -- check-invariants` — `^postulate` / Unsafe-named-module allowlist (postulates only allowed in the substrate Unsafe module)
- `cabal run shake -- check-no-properties-in-runtime` — runtime modules must not import Properties (would pull lemmas into MAlonzo)
- `cabal run shake -- check-erasure` — `@0` erasure assumption that FFI Marshal.hs depends on (CANId proof slot compiled to `AgdaAny`; Timestamp newtype)
- `cabal run shake -- check-fidelity` — MAlonzo constructor-drift smoke test (binary FFI end-to-end)
- `cabal run shake -- check-ffi-exports` — diffs MAlonzo-mangled FFI export names against `haskell-shim/ffi-exports.snapshot`

Then [AGENTS.md § Step 4](AGENTS.md#step-4-implement-and-verify) defines the full 4-gate sequence around these (Agda gates → unit tests → lint gates → benchmarks); do not let this section drift from it.

**Resources**: [Agda Documentation](https://agda.readthedocs.io/), [Standard Library](https://agda.github.io/agda-stdlib/), [Agda Tutorial](https://agda.readthedocs.io/en/latest/getting-started/tutorial-list.html).

---

## Current Session Progress

For history (R6–R17, Path G, Phase 5.1, Phases A/B.1/B.1.x/B.2/B.3.a-c, B.3.d pre-gate, Layers 1–2, Layer 3 commits 3a/3b/3c.0–3c.4/3d.1–3d.3) see [PROJECT_STATUS.md](PROJECT_STATUS.md). Per-commit narratives for 3d.4–3d.8 + 3d.5.a–3d.5.d sub-phases live in [PROJECT_STATUS.md](PROJECT_STATUS.md) and [memory/project_b3d_universal_proof.md](memory/project_b3d_universal_proof.md). Resume notes / next-session entry point: [.session-state.md](.session-state.md).

**Current track:** Phase E (VAL_ promotion to `DBCSignal.valueDescriptions`) ⏳ in flight on branch `b3d-3d5-format-dsl`.  Plan A (single self-contained commit at E.12 closure) locked by user "Plan A is okay".  **E.1 ✅ DONE 2026-05-04** + **E.2 ✅ DONE 2026-05-06** + **E.3 ✅ DONE** + **E.4 ✅ DONE** + **E.5α ✅ DONE** + **E.5β ✅ DONE 2026-05-06** + **E.6 ✅ DONE 2026-05-07** + **E.7 ✅ DONE 2026-05-07** + **E.8 ✅ DONE 2026-05-08** + **E.9a ✅ DONE 2026-05-08** — all ten closures uncommitted across session boundaries per Plan A (E.1→E.12 bundled).  E.10-E.12 pending.  Module count 237 → 240 → 242 → **243** at E.8 (E.9a 0 new files).  Resume via `.session-state.md`; full plan in `memory/project_phase_e_val_promotion.md`.

**Status (2026-05-08): Phase E.9a closed (uncommitted, gates green).**  Phase E.9a = lift E.1 interim `vds-empty`/`vds-eq` clauses + replace E.7's vacuous `tvd-WF` arm with non-vacuous derivation from signal-name validity via a `clearVds`/`clearVdsMsg` structural-transform cascade.  0 NEW Agda modules + 9 modified.  **The strategy** (advisor 2026-05-07): per-signal `attachToSignal (collectFromMessages msgs) s` does NOT recover `s` exactly — it overwrites `s.valueDescriptions` with the first matching `(canID, signalName)` lookup; both lookup-hit and lookup-miss reduce to `clearVds s` (= `record s { vds = [] }`).  This means the proof claim must shift from `≡ s` to `≡ clearVds s` at the leaf level (`buildSignal-fields-recover`), then cascade up — every per-message claim becomes `≡ clearVdsMsg msg`, every per-list claim becomes `≡ map clearVdsMsg msgs`, and the top-level Universal claim closes via `attachValueDescs (collectFromMessages msgs) msgs ≡ msgs` against the post-cascade RHS `map clearVdsMsg (DBC.messages d)`.  **The work — 9 files**: `Types.agda` adds `clearVds` + `clearVdsMsg` helpers + `Data.List.{[]; map}` import; `Refine/ValueDescriptions.agda` adds clearVds bridge section (~+250 LOC) — `attachWithMaybe-clearVds-on-singletonFromVds`, `attachToSignal-clearVds-on-collected`, `attachToMessage-clearVdsMsg-on-collected` (with private `map-map-fusion-clearVds`), `attachValueDescs-on-clearVdsMsgs-collectFromMessages` (with private `map-map-fusion-clearVdsMsg`), `map-attachToMessage-on-clearVdsMsgs-collected` — plus non-vacuous tvd-WF derivation (`ident-name-as-vd-name-stop` reuses `identifier-name-stop` via Σ-shape unification; `prependVdsRvd-stops`, `collectFromSignals-stops`, `collectFromMessages-stops`, private `All-stops-++`) plus unresolvedRVDs-clearVds variant (`matchesSigᵇ-clearVds`/`-msg`/`-msgs`, `unresolvedRVDs-clearVdsMsg-eq`, `unresolvedRVDs-on-clearVdsMsgs-collectFromMessages`) and removes ~7 obsolete vacuous helpers (`attachToSignal-empty`, `attachToMessage-empty`, `attachValueDescs-empty`, `map-attachToMessage-empty`, the empty-form `map-attachToMessage-on-collected`, `collectFromSignals-vds-empty`, `collectFromMessages-vds-empty`); `Topology/Resolve.agda` drops `vds-eq` precondition + threads `clearVds` (`buildSignal-fields-recover` claim `≡ sig` → `≡ clearVds sig`; `SigOK` ctors lose `vds-eq`; `buildSignal-roundtrip-Always`/`-When` return `just (clearVds sig)`; `buildAllRaw-roundtrip` returns `just (map clearVds sigs)`; `all-sigOK-no-mux`/`-mc-mux` drop input lists; `resolveSignalList-roundtrip` returns `just (map clearVds sigs)`); `Topology/Message.agda` drops `MessageWF.vds-empty` field + threads `clearVdsMsg` (buildMessage-roundtrip / parseMessage-roundtrip / parseMessage-roundtrip-bundled return `mkResult (clearVdsMsg msg) ...`; `parseMessages-roundtrip` switched from `many-η-roundtrip` to `many-η-roundtrip-with-lift` with `L = clearVdsMsg` so the result is `mkResult (map clearVdsMsg msgs) ...`); `Formatter/WellFormedText.agda` removes `WellFormedTextSignal.vds-empty` field; `Aggregator/Foundations.agda` makes the load-bearing single-line edit `liftTopStmt _ (TM m) = TSMessage (clearVdsMsg m)` — this is the **single proof-only fork point** that propagates structurally via `cong`/`trans` through every downstream proof; `Aggregator/Dispatcher/Simple/Message.agda` updates `parseTopStmt-on-emit-TM-eq` claim/internals to `clearVdsMsg msg`; `Aggregator/Partition.agda` cascades `clearVdsMsg` through `partition-onto-acc-TM` / `partitionTopStmts-bridge` (first arg of `mkCollectedTop` now `map clearVdsMsg (DBC.messages d)`) / `acc-M` / `final-eq` / `compose`; `Aggregator/Universal.agda` removes `msgs-vds-empty` extractor + subst-based vacuous `tvd-WF`, replaces with non-vacuous `tvd-WF = all-map TVD ... (lift-stops TVD ... (collectFromMessages-stops msgs) wfTVD)`, and switches `finalizeParse-on-mkResult-clean` to the `map-attachToMessage-on-clearVdsMsgs-collected` + `unresolvedRVDs-on-clearVdsMsgs-collectFromMessages` populated forms; `TextParser.agda` doc comment refresh.  **Coverage upgrade**: text-roundtrip universal `parseText (formatText d) ≡ inj₂ d` now holds for arbitrary DBCs modulo the *other* `WellFormedDBC` fields.  E.7's coverage caveat ("vacuously for non-empty vds") is resolved.  E.10's FFI export will now genuinely emit VAL_ for arbitrary inputs (no `WellFormedTextSignal.vds-empty` constraint anywhere); E.12's empirical test exercises non-vacuously.  **Architectural patterns established at E.9a**: (a) **`liftTopStmt` is the single fork point for proof-only structural transformations** — a single edit at one branch of this proof-only dispatch point cascades mechanically to every downstream proof via Agda's `cong`/`trans` propagation through index expressions; the E.9a cascade was ~9 files / ~300 LOC end-to-end, well under the original ~500 LOC budget.  (b) **Σ-shape unification reuses upstream lemmas** — `IdentNameStop` and `RawValueDescNameStop` both decompose `Identifier.valid` to identical Σ-types, so `ident-name-as-vd-name-stop` reuses the existing `identifier-name-stop` proof at zero LOC cost.  (c) **clearVds bridge dual-form** mirrors E.7's pattern: ship `attachValueDescs-on-clearVdsMsgs-collectFromMessages` (universal target after the cascade) AND `map-attachToMessage-on-clearVdsMsgs-collected` (the unfolded `map (attachToMessage rvds)` shape per `feedback_rewrite_through_record_builder`).  (d) **`feedback_chunk_structure_cascade` discipline applied upfront** — grep'd every `vds-empty`/`vds-eq` consumer (Resolve.agda / Message.agda / Universal.agda / WellFormedText.agda / Refine/ValueDescriptions.agda) BEFORE writing proof code; cascade fully enumerated up-front; no surprise walker mid-session.  Memo justified its existence.  **Module count: 243 unchanged** (E.9a was pure modification; the +1 over post-E.7's 242 was E.8's prior `Formatter/WellFormedText/ValueDescResolves.agda`).  **Gates passed**: `cabal run shake -- build` 2m08s; `cabal run shake -- check-properties` 8m17s ("All proof modules type-checked successfully!"); `cabal run shake -- check-invariants` clean; `cabal run shake -- check-no-properties-in-runtime` clean; `cabal run shake -- check-erasure` 1m43s; `cabal run shake -- check-fidelity` 1m56s; `cabal run shake -- check-ffi-exports` clean (11 entries); `cabal run shake -- count-modules` 243; Python `pytest tests/` 751p+1s in 2.09s (snapshots byte-identical — proof-side only); Go `go test ./aletheia/ -count=1 -race` 3.235s; C++ `ctest --test-dir cpp/build` 8/8 in 23.82s; `basedpyright` 0/0/0; `pylint aletheia/` 10.00/10; `gofmt -l . && go vet ./...` clean.  **Lessons captured this session**: no new feedback memos.  Patterns reused: `feedback_chunk_structure_cascade` (applied upfront — grep'd all consumers BEFORE writing proof code; no surprise walker), `feedback_rewrite_through_record_builder` (clearVds bridge's dual-form mirrors E.7's pattern), `feedback_proof_gate_is_check_properties` (8m17s gate is the source of truth, not `build`), `feedback_no_unilateral_deferral` (Plan A holds, no E.9a commit), `feedback_pre_commit_scope_check` (verified diff ships stated coverage upgrade — no half-finished implementations).  **NO COMMIT** at user direction; UPD doc-state sync only.  Active branch: `b3d-3d5-format-dsl`.

**Status (2026-05-08): Phase E.8 closed (uncommitted, gates green).**  Phase E.8 = `WellFormedText.ValueDescResolves` predicate for E.11's CHECK 23 prerequisite.  1 NEW Agda module + 4 modified.  **NEW** `src/Aletheia/DBC/Formatter/WellFormedText/ValueDescResolves.agda` (~75 LOC) — defines `ResolvesValueDesc : DBC → RawValueDesc → Set` (Σ-witness: `Σ[ msg ∈ DBC.messages d ] (DBCMessage.id msg ≡ rvd.canId × Σ[ sig ∈ DBCMessage.signals msg ] (DBCSignal.name sig ≡ rvd.signalName))`) + `WellFormedTextDBC.vds-resolve : All (ResolvesValueDesc d) (collectFromMessages (DBC.messages d))`.  **4 modified**: `Formatter/Properties.agda` (re-export facade so consumers see the predicate at the canonical Formatter.Properties surface); `Formatter/MetadataRoundtrip.agda` + `JSONParser/Properties.agda` (added `any-validNodeName` projection helper — companion clean-up landed here while plumbing the predicate); `python/aletheia/client/_helpers.py` (`validNodeName` predicate exposed binding-side; consumed by validator harness — wires to CHECK 23 at E.11); `Shakefile.hs` (added `WellFormedText/ValueDescResolves` to `check-properties` walk roots — without this entry the new module would silently bit-rot per `feedback_check_properties_aggregator_walks`).  **Module count: 242 → 243** (+1 NEW, `--safe --without-K`); breakdown: 238 `--safe --without-K` + 4 `--safe --without-K --no-main` + 1 allowlisted `--without-K`-only Substrate.Unsafe.  242 of 243 modules use `--safe`.  **Gates passed**: `cabal run shake -- check-properties`, `build`, `check-invariants`, `check-no-properties-in-runtime`, `check-erasure`, `check-fidelity`, `check-ffi-exports`, `count-modules` 243, Python `pytest tests/`, Go `go test ./aletheia/`, C++ `ctest`.  **NO COMMIT** at user direction.  Active branch: `b3d-3d5-format-dsl`.

**Status (2026-05-07): Phase E.7 closed (uncommitted, gates green).**  Phase E.7 = text formatter wiring + universal-proof vacuous closure.  0 NEW Agda modules + 8 modified.  **The work**: `TextFormatter/ValueTables.agda` adds `emitValueDescriptions-rvds-chars : List RawValueDesc → List Char` (foldr fold) + wrapper `emitValueDescriptions-chars msgs = emitValueDescriptions-rvds-chars (collectFromMessages msgs)`; `TextFormatter/TopLevel.agda` inserts the section between `emitMessages-chars` and `emitEnvVars-chars` in `formatChars` (canonical position 7 in the 11-section order, post-BO_, pre-EV_); `Foundations.agda` extends `toTopStmtsTyped` 6→7 chunks with `map TVD (collectFromMessages (DBC.messages d))` between TM and TEV; `Partition.agda` flips `partitionTopStmts-bridge` RHS 7th arg from `[]` to `collectFromMessages (DBC.messages d)`, inserts `acc-TVD` between `acc-EV` and `acc-M` (inside-out ordering since TVD sits between TM and TEV in toTopStmtsTyped), extends `map-distrib` / `foldr-app-bridge` / `final-eq` to handle the 7th chunk (calls existing E.5β `partition-onto-acc-TVD` infrastructure); `BodyBridge.agda` extends `formatChars-body` to 7 sections + adds `emit-map-TVD-eq` per-section bridge + extends `formatChars-body-bridge` from 5 step lemmas to 6 + 6 cong layers in distrib + 6 substitutions in convert; `Universal.agda` swaps `map-attachToMessage-empty` for `map-attachToMessage-on-collected` in `finalizeParse-on-mkResult-clean` + adds `tvd-WF` arm in `toTopStmtsTyped-WF`'s where-block (derives `All (TopStmtTypedWF defs) (map TVD (collectFromMessages msgs))` vacuously via `subst (All P) (sym (cong (map TVD) (collectFromMessages-vds-empty msgs ...))) []`) + adds `msgs-vds-empty` extractor (`All.map MessageWF.vds-empty (msg-wfs wf)`); `Refine/ValueDescriptions.agda` adds `map-attachToMessage-on-collected` (alias for `attachValueDescs-on-collectFromMessages` with the unfolded `map (attachToMessage rvds)` LHS shape per `feedback_rewrite_through_record_builder`) + `collectFromSignals-vds-empty` + `collectFromMessages-vds-empty` helpers (proves `collectFromMessages msgs ≡ []` under per-signal vds-empty; used by Universal's `tvd-WF`).  **Coverage caveat (advisor 2026-05-07)**: E.7's universal proof closes **vacuously** for non-empty vds — `MessageWF.vds-empty` is still in scope (E.1 interim clause), so `WellFormedDBC d` only holds for d's whose signals all have empty vds; `collectFromMessages msgs = []` for such d's, and the proof closes via `collectFromMessages-vds-empty`.  The new `map-attachToMessage-on-collected` and `tvd-WF` are wired in but not exercised non-vacuously.  E.9 lifts the proof, E.10 exposes the FFI, E.12 lands the empirical test.  Phrase E.7's deliverable as "type-level wiring + vacuous proof closure", NOT "VAL_ formatter empirically validated".  **Architectural patterns established at E.7**: (a) **Vacuous-via-WF-restrictive-predicate** for staged proof rollout — when adding a new chunk whose universal proof requires a non-trivial inverse-bridge but the WF predicate is more restrictive than the bridge needs, discharge the new chunk's All-precondition vacuously by showing the chunk reduces to `[]` under the restrictive predicate; lift the predicate later (E.9) to expand coverage. (b) **TextFormatter → TextParser dependency direction** — pre-E.7 the TextFormatter modules only depended on `Aletheia.DBC.Types`; E.7 adds `TextFormatter.ValueTables → TextParser.ValueDescriptions` for `collectFromMessages`. No cycle introduced (TextParser.ValueDescriptions only depends on Types + TextParser.ValueTables; no Properties, no TextFormatter). (c) **Chunk-structure cascade** — extending `toTopStmtsTyped` 6→7 chunks cascaded to `Universal.toTopStmtsTyped-WF` (the implicit walker the original session-state plan didn't enumerate); caught by Agda type-error in 30 seconds.  Now captured as `memory/feedback_chunk_structure_cascade.md` for future Path-X chunk additions; useful at E.9 (vds-empty removal cascades through Resolve / Message / Universal at minimum).  **Module count: 242 unchanged** (no new files in E.7).  **Gates passed**: full `cabal run shake -- build` 1m55s (libaletheia-ffi.so rebuilt, no streaming-hot-path change since `attachValueDescs (collectFromMessages msgs) msgs` runs at DBC build time only); `cabal run shake -- check-properties` 8m01s ("All proof modules type-checked successfully!"); `cabal run shake -- check-invariants` clean; `cabal run shake -- check-no-properties-in-runtime` clean (TextFormatter→TextParser is runtime→runtime, not runtime→Properties); `cabal run shake -- check-erasure` 1m44s; `cabal run shake -- check-fidelity` 1m44s (binary FFI smoke); `cabal run shake -- check-ffi-exports` clean (11 entries); `cabal run shake -- count-modules` 242; Python `pytest tests/` 751p+1s in 2.07s (snapshots byte-identical, no `ALETHEIA_UPDATE_SNAPSHOTS=1` needed — confirms advisor prediction that JSON-based parity tests don't exercise `formatChars`); Go `go test ./aletheia/ -count=1 -race` 3.24s; C++ `ctest --test-dir cpp/build` 8/8 in 23.82s including `dbc_corpus_parity_tests`.  **Lessons captured this session**: new feedback memo `memory/feedback_chunk_structure_cascade.md` (extending a sum-type chunk structure cascades to every walker function — `Universal.toTopStmtsTyped-WF` was the missed walker; original plan listed 6 file edits, advisor and I both missed the 7th).  Patterns reused: `feedback_rewrite_through_record_builder` (cong-through-buildDBC drives `map-attachToMessage-on-collected` choice over `attachValueDescs-on-collectFromMessages`), `feedback_proof_gate_is_check_properties` (8m gate is the source of truth, not `build`), `feedback_no_unilateral_deferral` (Plan A holds, no E.7 commit), `feedback_pre_commit_scope_check` (verified diff ships stated goal — caught the missed `tvd-WF` arm before declaring done).  **NO COMMIT** at user direction; user said "Resume" not "UPD" so no doc commit either.  Active branch: `b3d-3d5-format-dsl`.

**Status (2026-05-07): Phase E.6 closed (uncommitted, gates green).**  Phase E.6 = VAL_ refinement: `attachValueDescs` runtime + inverse-bridge proof + universal-aggregator wiring.  2 NEW Agda modules (657 LOC) + 4 modified.  Came in well under CM_-class budget (advisor-estimated 1500-2500 LOC).  **2 NEW**: (a) `src/Aletheia/DBC/TextParser/ValueDescriptions.agda` (146 LOC) — runtime: `lookup-vd : CANId → Identifier → List RawValueDesc → Maybe RawValueDesc` (Bool predicate `matchesVD` + `if`-chain), `attachWithMaybe` (Maybe-elim helper, pattern-matches on `mkRawValueDesc` per `feedback_k_elim_constructor_records`), `attachToSignal` / `attachToMessage` / `attachValueDescs = map (attachToMessage rvds)`, `prependVdsRvd` (factored case-analysis helper splitting `with DBCSignal.valueDescriptions s` outside the function body), `collectFromSignals` / `collectFromMessage` / `collectFromMessages = concatMap collectFromMessage`.  Excludes empty-vds signals at collect time (matches E.7's planned formatter behavior).  (b) `src/Aletheia/DBC/TextParser/Properties/Aggregator/Refine/ValueDescriptions.agda` (511 LOC) — proof: 4 decidable-equality micro-lemmas + `singletonFromVds` Maybe-elim helper + 3 `prependVdsRvd-*` helpers (canId-all + lookup-on-prepend variants for name-eq / name-≢) + 6 lookup discipline helpers (skip-prefix, on-all-canId-≢, name-not-in-collectFromSignals, just-prefix, nothing-prefix, all-++-canId-≢) + 2 ∈-map bridges + 2 AllPairs-head shape converters (flips ≢ direction) + **Layer A** `lookup-vd-on-collectFromSignals-found` (within-msg) + **Layer 1** `lookup-on-collected` (cross-msg, the crux) + **Layer 2** `attachWithMaybe-on-singletonFromVds` + `attachToSignal-on-collected` + **Layer 3** `attachToMessage-on-collected` (via `map-≡-id`) + **Layer 4** `attachValueDescs-on-collectFromMessages` (the universal target) + **Vacuous-empty** `attachValueDescs-empty` + `map-attachToMessage-empty` (unfolded variant for rewrite-through-buildDBC).  **4 modified**: `Properties/Topology/Message.agda` (`MessageWF.sig-names-unique : AllPairs _≢_ (map .name signals)` — cascade analysis confirmed zero since `MessageWF` consumed only via projection in `parseMessage-roundtrip-bundled`); `Properties/Aggregator/Universal.agda` (`WellFormedDBC.msg-ids-unique` + `finalizeParse-on-mkResult-clean` switched to `cong (λ msgs → inj₂ (record d { messages = msgs })) (map-attachToMessage-empty (DBC.messages d))` since `rewrite` couldn't fire through `buildDBC`'s opacity — captured pattern in new `feedback_rewrite_through_record_builder.md`); `TextParser.agda` (`buildDBC` applies `attachValueDescs (CollectedTop.rawValueDescs c) (CollectedTop.messages c)` at the messages-field of the record literal); `TextParser/ValueDescriptions.agda` (refactored `collectFromSignals` from inline `with` to `prependVdsRvd` helper to avoid `with`-abstraction trap).  **Architectural patterns established at E.6**: (a) **`prependVdsRvd` factoring** — when a function does `with f x | [] = A | x:xs = B`, factor `f x` to a top-level helper that takes the scrutinee as a parameter; separates case-analysis from outer call so proofs can access the case-analyser by name (per `feedback_with_abstraction_traps`).  (b) **Uniqueness as new WF field, not wrapper record** — `MessageWF.sig-names-unique` and `WellFormedDBC.msg-ids-unique` were ADDED to existing records; cascade was zero because consumed only via projection.  Advisor's "wrapper record fallback" was unnecessary.  (c) **`rewrite` doesn't fire through opaque function applications** — `attachValueDescs-empty` couldn't fire because Agda kept `buildDBC` opaque in the goal; switch to `cong (λ field → … record d { field = … })` to apply the equation at the field-position directly.  See new memo `memory/feedback_rewrite_through_record_builder.md`.  (d) **Vacuous-empty + universal-target dual** — ship both `attachValueDescs-empty` (E.6 closure form) and `attachValueDescs-on-collectFromMessages` (E.7's needed form); E.7 just swaps the cong's equation argument.  **Module count: 240 → 242** (+2 NEW, all `--safe --without-K`); breakdown: 237 `--safe --without-K` + 4 `--safe --without-K --no-main` + 1 allowlisted `--without-K`-only Substrate.Unsafe.  **Gates passed**: full `cabal run shake -- build` 1m57s; `cabal run shake -- check-properties` 8m12s ("All proof modules type-checked successfully!"); `cabal run shake -- check-invariants` clean; `cabal run shake -- check-erasure` 1m43s; `cabal run shake -- check-fidelity` 1m46s; `cabal run shake -- check-ffi-exports` clean (11 entries); `cabal run shake -- check-no-properties-in-runtime` clean; `cabal run shake -- count-modules` 242; Python `pytest tests/` 751p+1s with 8 parity snapshots refreshed via `ALETHEIA_UPDATE_SNAPSHOTS=1` (VAL_ entries now distributed onto the right signals — confirmed visually for `EngineState [Off, Cranking, Running, Stall]` in `value_tables.json`); Go `go test ./aletheia/ -count=1 -race` 3.1s; C++ `ctest --test-dir cpp/build` 8/8 in 22.47s including `dbc_corpus_parity_tests`.  **Lessons captured this session**: new feedback memo `memory/feedback_rewrite_through_record_builder.md` (rewrite-vs-cong dispatch when goal has `… buildX (…) … ≡ d` shape).  Patterns reused: `feedback_with_abstraction_traps` (`prependVdsRvd` factoring), `feedback_expose_scrutinee_for_external_rewrite` (`singletonFromVds` Maybe-elim), `feedback_proof_gate_is_check_properties` (verified gates via `check-properties`, not `build`), `feedback_no_unilateral_deferral` (refresh snapshots immediately rather than defer per the test docstring's documented workflow), `feedback_k_elim_constructor_records` (constructor-pattern matched on `mkRawValueDesc` at outer level).  **NO COMMIT** at user direction; UPD doc-state sync only.  Active branch: `b3d-3d5-format-dsl`.

**Status (2026-05-06): Phase E.5β closed (uncommitted, gates green).**  Phase E.5β = per-section Format DSL roundtrip + real TVD dispatcher proof + partition + CollectedTop extension.  3 NEW Agda modules (~480 LOC) + 8 modified.  **3 NEW**: (a) `src/Aletheia/DBC/TextParser/Format/ValueDescription.agda` — Format DSL Format for VAL_ lines.  Carrier `(ℕ × Identifier × List (ℕ × List Char))` (RAW triple, NOT decoded — `iso` requires total bijection but `buildCANId : ℕ → Maybe CANId` is partial; partial decode lives outside the DSL via wrapper).  Reuses `ValueEntry-format` and `build-EmitsOKMany` from `Format.ValueTable`.  `RawValueDescNameStop` precondition asserts the signal name's first char is non-isHSpace.  `build-emits-ok-vd` constructs the EmitsOK certificate via 2 cases (empty / non-empty entries).  Top-level `parseValueDescription-format-roundtrip` discharges parse-after-emit via one `roundtrip` call.  (b) `src/Aletheia/DBC/TextParser/Properties/ValueTables/ValueDesc.agda` — slim `parseValueDescription-roundtrip` derived from the DSL universal.  Composes 4 steps: emit-bridge (DSL emit ≡ existing emitter via `cong` over `emit-many-eq-foldr` reused from VAL_TABLE_), DSL universal, `manyHelper-parseNewline-exhaust` for trailing blanks, `cong (buildResultP _ _) (buildCANId-rawCanIdℕ rvd.canId)` rewrites the Maybe to `just rvd.canId`, then `buildResultP (just rvd.canId) … = pure rvd` reduces by record-η.  `RawValueDescStop` alias re-exported.  (c) `src/Aletheia/DBC/TextParser/Properties/Aggregator/Dispatcher/Simple/ValueDesc.agda` — TVD bucket dispatcher `parseTopStmt-on-emit-TVD-eq`.  Three steps: head-dispatch reduces parseTopStmt to parseTopStmt-V (`refl`), `parseValueTable-fails-on-VAL_-prefix` proves the LEFT arm fails (`refl` because `parseCharsSeq "VAL_TABLE_"` fails on the 5th char ' ' vs 'T'), then `bind-nothing` propagates and `alt-right-nothing` reduces the `<|>`, then `bind-just-step` over the slim roundtrip.  **8 modified**: `ValueTables.agda` (parseValueDescription refactored to DSL form `parse ValueDescription-format >>= λ triple → many parseNewline >>= λ _ → buildResultP (buildCANId (proj₁ triple)) …`; `buildResultP` hoisted from where-bound to top-level Maybe-elim helper for proof-side `bind-just-step`'s same-function identity requirement); `TopLevel.agda` (CollectedTop gains `rawValueDescs : List RawValueDesc` 7th field, emptyCollected to 7-arity, consTop (TSValueDesc rvd) flips from drop to record-update on rawValueDescs); `Properties/Aggregator/Dispatcher.agda` (added `wfTVD : ∀ rvd → RawValueDescStop rvd → TopStmtTypedWF defs (TVD rvd)` ctor + replaced E.5α absurd-elim with real dispatcher proof); `Properties/Aggregator/Dispatcher/Simple.agda` (re-exports new submodule); `Properties/Aggregator/Partition.agda` (added `partition-onto-acc-TVD` lemma + extended `partitionTopStmts-bridge` RHS to 7-arity `mkCollectedTop … sgs []` — empty-empty `[]` is by construction since `toTopStmtsTyped` is still 6 chunks at E.5β; cong-mkCollectedTop 6→7 args; final-eq adds 7th `refl`); `Properties/ValueTables.agda` (facade re-exports new submodule).  **Architectural patterns established at E.5β** (worth flagging for E.6 / future): (a) **DSL doesn't handle partial decode** — Format yields raw triple, wrapper layers `buildResultP (buildCANId rawId)` outside the DSL.  (b) **Hoist where-bound helpers when proofs need them** — `bind-just-step` requires SAME function on both sides; where-bound helpers have unique identity that doesn't unify with external mirrors.  (c) **Maybe-elim direct pattern matching > `with`-on-Maybe** — per `feedback_expose_scrutinee_for_external_rewrite`, the constructor-pattern `buildResultP {nothing,just _} = …` reduces externally via `cong (buildResultP _) eq` while the `with`-form hides the scrutinee inside elaborated aux.  (d) **Empty-empty `[]` field at E.5β closure** — `partitionTopStmts-bridge` RHS now has `rawValueDescs = []` (7th arg) by construction; E.7 flips this to `collectFromMessages (DBC.messages d)`.  **Forward-looking note for E.7** (per advisor): universal proof at E.7 will need to construct `wfTVD rvd` for each rvd in the new TVD chunk; `RawValueDescStop` must flow from `WellFormedDBC d` — either (a) new `vd-stops` field, or (b) derive from existing `MessageWF` signal-name validity (preferable; file with C2 advisor consultation before E.6).  **Module count: 237 → 240** (+3 NEW, all `--safe --without-K`); breakdown: 235 `--safe --without-K` + 4 `--safe --without-K --no-main` + 1 allowlisted `--without-K`-only Substrate.Unsafe.  **Gates passed**: full `cabal run shake -- build` 1m58s; `cabal run shake -- check-properties` 8m03s ("All proof modules type-checked successfully!"); `cabal run shake -- check-invariants` clean; `cabal run shake -- check-erasure` clean; `cabal run shake -- check-fidelity` 3m16s; `cabal run shake -- check-ffi-exports` clean (11 entries); `cabal run shake -- check-no-properties-in-runtime` clean; `cabal run shake -- count-modules` 240; Python `pytest tests/` 751p+1s; Go `go test ./aletheia/ -count=1 -race` 3.2s; C++ `ctest --test-dir cpp/build` 8/8 in 23.44s.  **Lessons captured this session — no new feedback memos.**  Patterns reused: `feedback_expose_scrutinee_for_external_rewrite` + `feedback_with_abstraction_traps` + `feedback_proof_gate_is_check_properties` + `feedback_no_unilateral_deferral`.  **NO COMMIT** at user direction; UPD doc-state sync only.  Active branch: `b3d-3d5-format-dsl`.

**Status (2026-05-06): Phase E.3 / E.4 / E.5α closed** (three prior sessions between the E.1+E.2 entry below and the E.5β entry above; rolled up here for compactness).  **E.3 (binding-side JSON unwrap) closed**: 6 binding files modified — `python/aletheia/protocols.py` adds `valueDescriptions: NotRequired[list["DBCValueEntry"]]` on `DBCSignalAlways` + `DBCSignalMultiplexed`; `cpp/include/aletheia/dbc.hpp` hoists `DbcValueEntry` above `DbcSignal` and gives `DbcSignal` a `value_descriptions` field; `cpp/src/json_serialize.cpp` + `cpp/src/json_parse.cpp` emit/parse the field between `receivers` and presence; `go/aletheia/dbc.go` adds `DbcSignal.ValueDescriptions []DbcValueEntry`; `go/aletheia/json.go` symmetric serialize/parse.  Cross-binding parity tests pass for the field; module count unchanged at **237** — Agda-side untouched at E.3.  **E.4 (text-parser flip) closed**: 8 Agda files modified — `TextParser/ValueTables.agda` adds `record RawValueDesc { canId : CANId; signalName : Identifier; entries : List (ℕ × List Char) }` and flips `parseValueDescription` from `Parser ⊤` to `Parser RawValueDesc` with `buildCANId` fail-on-out-of-range; `TextParser/TopLevel.agda` flips `TSValueDesc : TopStmt` to `TSValueDesc : RawValueDesc → TopStmt`; `parseTopStmt-V` right arm changes from `parseValueDescription *> pure TSValueDesc` to `parseValueDescription >>= λ rvd → pure (TSValueDesc rvd)`; consTop (TSValueDesc _) c = c still drops at E.4 — collection deferred to E.5; `Properties/Aggregator/Dispatcher/Simple/ValueTable.agda` updates `alt-left-just` right-arm shape (proof body unchanged because `alt-left-just` is polymorphic in right-arm structure); `Properties/Aggregator/Foundations.agda` re-scopes `TopStmtTyped` comment block; 4 stale doc-comment cleanups (`TextFormatter/ValueTables.agda`, `TextParser/ExtendedMux.agda`, `TextParser/ValueTypes.agda`, `Formatter/WellFormedText.agda`).  Module count unchanged at **237** — no new modules at E.4.  Behavioral: `parseValueDescription` was previously parse-and-drop returning `⊤`; now builds a `RawValueDesc` and rejects whole-file on `buildCANId` failure.  All corpus fixtures use small standard IDs that decode cleanly.  **E.5α (typed shadow + emitter) closed**: 3 Agda files modified, 0 new modules — `TextFormatter/ValueTables.agda` adds `emitValueDescription-chars : RawValueDesc → List Char` (~10 LOC); `Properties/Aggregator/Foundations.agda` adds `TVD : RawValueDesc → TopStmtTyped` ctor + extends `liftTopStmt` and `emitTopStmt-chars`; `Properties/Aggregator/Dispatcher.agda` extends three functions — TVD case via absurd-elim (`()` on the WF witness, since `TopStmtTypedWF defs (TVD _)` is uninhabited at E.5α with no `wfTVD` ctor; E.5β adds the ctor and replaces the absurd-elim with a real proof), `emitTopStmt-chars-nonzero` adds trivial TVD case, `emitTopStmt-chars-head-not-newline` adds trivial TVD case.  `toTopStmtsTyped` deliberately left at 6 chunks per advisor's E.5/E.7 boundary: TVD reaches the typed shadow but is not yet emitted from `formatChars` — wires in at E.7 via `collectFromMessages (DBC.messages d)`.  **E.5α gate strategy** (absurd-elim over real proof): bypassed E.5β's substantial DSL build (~600 LOC) at E.5α by using missing-ctor uninhabitedness, then upgraded to a real proof at E.5β.  All E.3 / E.4 / E.5α gates green at their respective session closures (`build` + `check-properties` + `check-invariants` + binding tests); all uncommitted across session boundaries per Plan A.

**Status (2026-05-06): Phase E.2 closed (uncommitted, gates green).**  Phase E.2 = JSON wire layer.  4 Agda files modified + 8 parity snapshots refreshed: `src/Aletheia/DBC/JSONParser.agda` (parseObjectList + parseValueEntry + parseValueEntryList hoisted up above parseSignalFields; new `parseOptionalArray parseValueEntryList (lookupArray "valueDescriptions" obj) >>=ₑ λ vds →` bind threaded through; `valueDescriptions = []` placeholder replaced with `valueDescriptions = vds`); `src/Aletheia/DBC/JSONParser/SignalWF.agda` (`postPresence-wf×pv` + `parseSignalFields-wf×pv` both take `vds` after `receivers`; record literal in postPresence statement uses `valueDescriptions = vds`; downstream eq numbering shifted to eq₁₃/eq₁₄/eq₁₅ to accommodate the new with-step); `src/Aletheia/DBC/Formatter.agda` (`formatValueEntry` hoisted above `formatDBCSignal`; `formatDBCSignal` emits `("valueDescriptions" , JArray (map formatValueEntry (DBCSignal.valueDescriptions sig)))` between `receivers` and `formatPresence`); `src/Aletheia/DBC/Formatter/SignalRoundtrip.agda` (private `mkSignal` widens to 7-arg with `vds : List (ℕ × List Char)`; `signalFields` emits the new entry; `signal-roundtrip-LE` and `signal-roundtrip-BE` add `vds` between `rs` and `dwf` across 4 case-clauses (LE×Always, LE×When, BE×Always, BE×When), with `valueEntryList-roundtrip vds` rewrite added to each chain; `signal-roundtrip-go` takes `vds`; top-level `signal-roundtrip` passes `DBCSignal.valueDescriptions sig` through; imports `formatValueEntry` from Formatter and `valueEntryList-roundtrip` from MetadataRoundtrip).  **8 parity snapshots refreshed** (`python/tests/fixtures/dbc_corpus/parity_snapshots/{attributes,comments_groups,env_vars,extended_can,kitchen_sink,minimal,multiplexing,value_tables}.json`) via `ALETHEIA_UPDATE_SNAPSHOTS=1 python3 -m pytest tests/test_dbc_corpus_parity.py` — the test docstring documents this workflow as the response to "an intentional Agda wire-shape change".  **Latent E.1 gap closed at E.2.**  The previous session had reported "Agda gates green" at E.1 closure on the strength of `cabal run shake -- build` alone.  In fact `signal-roundtrip` in `Formatter/SignalRoundtrip.agda:192-195` was broken: the private `mkSignal` helper hardcoded `valueDescriptions = []` while the universal `signal-roundtrip` claim took an arbitrary `sig : DBCSignal`; closure required `sig.valueDescriptions ≡ []` which `WellFormedSignal` doesn't constrain.  The error was invisible to `build` because Properties / *Roundtrip / *WF files are walk-rooted in the separate `check-properties` phony target only.  E.2's wire widening — `mkSignal` becomes 7-arg, `signalFields` emits the new field, JSON parser parses + rebuilds with `vds` — fixes the roundtrip universally, no constraint needed.  **Lesson captured**: new feedback memo `memory/feedback_proof_gate_is_check_properties.md`.  **CLAUDE.md "Pre-commit minimum" + AGENTS.md "Step 4" both updated this session** to explicitly list the 6 proof-side / invariant Shake gates that need to run for code changes (`check-properties`, `check-invariants`, `check-no-properties-in-runtime`, `check-erasure`, `check-fidelity`, `check-ffi-exports`); the prior framing of `build` "(includes check-invariants, check-no-properties-in-runtime, check-erasure)" in AGENTS.md was factually wrong since `build` only chains `check-stdlib-version`.  **Module count:** unchanged at **237** — confirmed via `cabal run shake -- count-modules`.  **Gates passed**: full `cabal run shake -- build` 1m56s; `cabal run shake -- check-properties` 8m35s ("All proof modules type-checked successfully!"; pre-existing warning on `Aletheia.DBC.TextParser.Properties.Attributes.Assign.Rel` ModuleDoesntExport — Layer 4c, unrelated to E.2); `cabal run shake -- check-invariants` clean; Python `pytest tests/` 751p+1s; `basedpyright` 0/0/0; `pylint` 10.00/10; Go `gofmt -l . && go vet ./...` clean.  **Expected-fail at E.2 close**: Go `TestDBCCorpusParity` 8/8 sub-tests fail with "parity drift" — Go's `DbcSignal` struct lacks `ValueDescriptions`, so `JSON → struct → JSON` round-trip in `canonicalDBCJSON` drops the field; refreshed snapshots include the field; mismatch is real.  Identical failure on C++ `dbc_corpus_parity_tests` (1 of 8 ctest targets fails; the other 7 pass).  Same root cause; E.3 is the natural fix.  Per `feedback_no_unilateral_deferral`: knowingly red, named cause, named owner step — no `t.Skip` / `[skip]` annotations.  **NO COMMIT** at user direction; UPD doc-state sync only.  Active branch: `b3d-3d5-format-dsl`.

**Status (2026-05-04): Phase E.1 closed (uncommitted, gates green).**  9 files in the working tree across the session boundary — `src/Aletheia/DBC/Types.agda` (added `valueDescriptions : List (ℕ × List Char)` as 7th field on `DBCSignal`); `src/Aletheia/DBC/JSONParser.agda` + `JSONParser/SignalWF.agda` + `Formatter/SignalRoundtrip.agda` + `TextParser/Topology/SignalLine.agda` (`valueDescriptions = []` placeholders at the 5 record-construction sites); `src/Aletheia/DBC/Properties/Equality.agda` (`_≟-DBCSignal_` extended to 7 fields with new private `_≟-vd_` for `(ℕ × List Char)` pairs); `src/Aletheia/DBC/Formatter/WellFormedText.agda` (`vds-empty : DBCSignal.valueDescriptions s ≡ []` field on `WellFormedTextSignal`); `src/Aletheia/DBC/TextParser/Properties/Topology/Resolve.agda` (`vds-eq` precondition threaded through `buildSignal-fields-recover`, `buildSignal-roundtrip-Always`, `buildSignal-roundtrip-When`, `SigOK`, `all-sigOK-no-mux`, `all-sigOK-mc-mux`, `resolveSignalList-roundtrip`); `src/Aletheia/DBC/TextParser/Properties/Topology/Message.agda` (`vds-empty` field on `MessageWF`; threaded through `buildMessage-roundtrip` + `parseMessage-roundtrip` + `parseMessage-roundtrip-bundled`).  **Architectural pattern that emerged**: when a record field needs a temporary `= some-default` constraint across many proofs (here: `valueDescriptions = []` until E.7 ships the formatter side), fold the constraint into the existing WF predicate at the natural layer (`WellFormedTextSignal.vds-empty` + `MessageWF.vds-empty`) rather than threading a precondition through every lemma; the substrate universal `parseText (formatText d) ≡ inj₂ d` continues to take `WellFormedDBC d` as hypothesis with no signature change at the top, propagating structurally via `WellFormedDBC.msg-wfs : All MessageWF (DBC.messages d)`; lower-level layers (`buildSignal-fields-recover`, `SigOK`, `resolveSignalList-roundtrip`) take individual `All`s extracted at `parseMessage-roundtrip-bundled`'s bundling boundary, and DO need explicit `vds-eq` plumbing.  E.7 / E.9 cleanup deletes the two record fields + their consumers.  **Module count:** unchanged at **237** — no new modules at E.1 (only field additions and proof-cascade edits).  **Gates passed**: Agda `Main.agda` clean; full `cabal run shake -- build` clean (libaletheia-ffi.so rebuilt in 2m03s); Python `pytest tests/` 751p+1s (no regression vs pre-E.1 baseline).  C++ / Go binding tests not yet run; verify on resume.  **NO COMMIT** at user direction; UPD doc-state sync only.  Three load-bearing constraints on the remaining E.2-E.12 work locked from advisor 2026-05-04 (full detail in `memory/project_phase_e_val_promotion.md`): C1 encounter-order preservation via locked sort key `(message-index, signal-index, val-desc-index)` baked into formatter's emit walk; C2 `attachValueDescs ∘ collectFromMessages ≡ id` is structurally novel vs `refineAttributes-on-rawOf` (CM_-class proof budget 1500-2500 LOC); C3 Python `dbc_to_text` defers to Agda via new FFI export `aletheia_format_dbc_text` — one verified emitter, no parallel pure-Python implementation.  Validator CHECK 23 `UnknownValueDescriptionTarget` required (mirrors CHECK 21 via `liftPerSignal`).  Active branch: `b3d-3d5-format-dsl`.

**Prior current track (now superseded by Phase E):** Phase D ✅ COMPLETE 2026-05-04 on branch `b3d-3d5-format-dsl` — cross-binding doc-example harness bound across all three bindings (R17-DEF-6). **D.2 (Go) ✅** (`d0ae26b`, 2026-05-04); **D.1 (C++) ✅** (`82d0347`, 2026-05-04); Python R17 C6 was the original. **R17-DEF-6 fully closed.**

**Status (2026-05-04): Phase D.1 COMPLETE.**  One ship commit: `82d0347` feat(D.1) — C++ Catch2 doc-example harness mirroring Python `pytest --markdown-docs` and Go `TestDocExamples`.  Every ```cpp``` fence in 5 tracked markdown files (`README.md` + `docs/PITCH.md` + `docs/architecture/CANCELLATION.md` + `docs/reference/INTERFACES.md` + `docs/development/DISTRIBUTION.md`) is now extracted, wrapped, compiled via `popen()` to `${CMAKE_CXX_COMPILER}`, and executed end-to-end as part of `ctest`'s default battery (registered as `doc_example_tests`, ~500 LOC).  **Harness shape** (`cpp/tests/doc_example_tests.cpp`): markdown walker emits one Catch2 `DYNAMIC_SECTION` per fence; three wrapper shapes mirror D.2 (full `int main()` verbatim / `#include` block + decls / body fragment with `using namespace aletheia;` and predeclared `client`/`initial_backend`/`backend`/`dbc`/`ts`/`can_id`/`canID`/`dlc`/`data_storage`/`data`/`frames`/`libPath` globals); per-fence subprocess compile bypasses CMake transitive resolution by passing static-archive paths through `target_compile_definitions` + `$<TARGET_FILE:...>` generator expressions (`aletheia-cpp` + `yaml-cpp` + `OpenXLSX`); path substitutions parallel D.2's `testdata/doc_examples/` (`/opt/aletheia/lib/libaletheia-ffi.so` → resolved via existing loader chain; `checks.yaml` → `cpp/tests/testdata/doc_examples/checks.yaml` 2-check fixture matching Go's; `checks.xlsx`/`tests.xlsx` → existing `examples/demo/demo_workbook.xlsx`); skips when `libaletheia-ffi.so` is missing.  **Structural gates** (in the same TU): `TestNoNotestCppFences` rejects `<!-- cpp notest -->` annotations (mirrors Python `python notest` ban + Go `<!-- go notest -->` ban) — non-runnable fences must use the `text` info string; collective `≥6` cpp fence floor.  **Doc edits — every `cpp` fence now compiles + runs against the real Agda core**: harness immediately surfaced 4 dead/wrong API references — `docs/PITCH.md` L42 + L157 (`PhysicalValue{220}` → `PhysicalValue{Rational{220, 1}}` since `PhysicalValue` ctor takes `Rational`); `docs/architecture/CANCELLATION.md` L84 (signature-only fence) + L228 (illustrative streaming worker referencing undefined `FrameProducer`/`handle_error`/`log_committed_count`/`properties_path`) flipped ```cpp``` → ```text```; `docs/reference/INTERFACES.md` L52 (`aletheia::check::signal` → `aletheia::Check::signal` since Check is a class with static method, not a namespace) + L63 (missing `aletheia::ltl::atomic` wrap + bare-expr prefix `[[maybe_unused]] auto _formula = `) + L74 (`aletheia::yaml::load_checks` → `aletheia::load_checks_from_yaml`) + L86 (`aletheia::excel::load_checks` → `aletheia::load_checks_from_excel`) + L99 (missing `std::stop_token{}` first arg on `client.send_frame` post-Phase-C.4 — parallel to Go L101 fix from D.2) + L665 (`f.key`/`f.value` → structured binding `[key, value]` + `std::visit` for `LogValue` variant printing since `LogField = std::pair<std::string_view, LogValue>`).  **`docs/FEATURE_MATRIX.yaml` flip**: `doc_example_gate_checks.cpp` `planned` → `implemented` (entry: `aletheia/client.hpp#AletheiaClient`, parallels Python's `aletheia.AletheiaClient` and Go's `Client`).  **Module count unchanged at 237** — no Agda changes (binding-side ergonomics over existing FFI surface, parallel to Phase C and Phase D.2).  **Gates passed**: Agda `check-invariants` clean; ctest 8/8 incl. doc_example_tests (~22s on cold cache, all tracked cpp fences compile + run); Go `go test ./aletheia/ -count=1 -race` clean (10 doc fences in ~1.0s, no regression from doc edits — D.2's harness re-validated PITCH.md/CANCELLATION.md/INTERFACES.md edits); Python `pytest tests/` 751p+1s; pylint 10.00/10; basedpyright 0/0/0; gofmt + go vet + clang-format + clang-tidy clean.  **Lessons captured**: no new feedback memories — the patterns are applications of existing memos (`feedback_doc_examples_are_gated`, `feedback_cross_language_parity`, `feedback_audit_all_wire_boundaries`, `feedback_no_unilateral_deferral`, `feedback_no_suppression_without_approval`); the 4 dead doc references the harness immediately caught are themselves the lesson — without an executable doc gate per language, fences silently rot through type-API drift across phases (Phase C.4's stop_token-first-arg refactor invalidated INTERFACES.md L99 just as Phase C.3's ctx-first-arg refactor had invalidated INTERFACES.md L101 on the Go side).  Notable subtlety: initial draft included `// NOLINT(google-build-using-namespace)` on `using namespace aletheia;` — advisor flagged the suppression; removing it surfaced that the check is project-wide disabled at `cpp/.clang-tidy:82` since Phase 5.1 (`a8ba94c`) with explicit comment "using namespace aletheia in tests is fine"; the NOLINT was redundant.  **R17-DEF-6 fully closed** with this commit.  Active branch: `b3d-3d5-format-dsl`.

**Status (2026-05-04): Phase D.2 COMPLETE.**  One ship commit: `d0ae26b` feat(D.2) — Go doc-example harness mirroring Python `pytest --markdown-docs`.  Every ```go``` fence in 5 tracked markdown files (`README.md` + `docs/PITCH.md` + `docs/architecture/CANCELLATION.md` + `docs/reference/INTERFACES.md` + `docs/development/DISTRIBUTION.md`) is now extracted, wrapped, and executed end-to-end via `go run` as part of the default `go test ./aletheia/` battery.  **Harness shape** (`go/aletheia/doc_examples_test.go`, ~310 LOC): markdown walker emits one subtest per fence; three wrapper shapes (full `package main` verbatim / `import (…)` block + decls / body fragment with predeclared `ctx`/`client`/`backend`/`dbc`/`ts`/`canID`/`dlc`/`data`/`frames`/`libPath` globals); path substitutions parallel Python `conftest.py`'s loader fakes (`/opt/aletheia/lib/libaletheia-ffi.so` → resolved .so via existing loader chain; `checks.yaml` → `testdata/doc_examples/checks.yaml` 2-check fixture; `checks.xlsx`/`tests.xlsx` → existing `examples/demo/demo_workbook.xlsx`); per-fence tempdir under `t.TempDir()` with shared `go.mod` carrying `replace` directives to local repo's two Go modules (`aletheia-go`, `aletheia-go/excel`); auto-suppresses unused-var compile errors via `go/parser` walk over top-level `:=` declarations; up-front prime build serializes module-cache lock once, then per-fence `go run` runs in parallel via `t.Parallel` + a `runtime.NumCPU()`-bounded semaphore (10 fences in ~1.0s wall clock); skips when `libaletheia-ffi.so` is missing.  **Structural gate** (`go/aletheia/doc_no_notest_test.go`): `TestNoNotestGoFences` rejects `<!-- go notest -->` annotations (mirrors Python `python notest` ban) — non-runnable fences must use the `text` info string; `TestEveryDocFileHasAtLeastOneGoFenceCollectively` enforces ≥8 fences total.  **Doc edits — every `go` fence now compiles + runs against the real Agda core**: harness immediately surfaced 4 dead/wrong API references — `docs/PITCH.md` L45 (prefix `aletheia.`) + L166 (rewrite buggy LoadChecks*/ltl.Always shape to real APIs); `docs/architecture/CANCELLATION.md` L66 (signature-only fence flipped ```go``` → ```text```) + L182 (import path correction); `docs/reference/INTERFACES.md` L66 (bare struct expr prefixed `_ = `) + L77 (`yaml.LoadChecks` → `aletheia.LoadChecksFromYAMLFile`) + L101 (missing `ctx context.Context` first arg on `client.SendFrame` — Phase C.3's ctx-first-arg refactor had invalidated the fence).  **`docs/FEATURE_MATRIX.yaml` flip**: `doc_example_gate_checks.go` `planned` → `implemented` (entry: `Client` to parallel Python's `aletheia.AletheiaClient`; the structural gate `collectGoSymbols` skips `_test.go` files so referencing the test function would have failed).  **Module count unchanged at 237** — no Agda changes (binding-side ergonomics over existing FFI surface, parallel to Phase C).  **Gates passed**: Agda `check-invariants` clean; Go `go test ./aletheia/ -count=1 -race` 3.2s (10 doc fences in ~1.0s, all matrix entries resolve); `gofmt` + `go vet` clean; Python `pytest tests/` 751p+1s; `pytest --markdown-docs` on the four edited files: 22 python fences pass.  **Lessons captured**: no new feedback memories — the patterns are applications of existing memos (`feedback_doc_examples_are_gated`, `feedback_cross_language_parity`, `feedback_no_unilateral_deferral`); the 4 dead doc references the harness immediately caught are themselves the lesson — without an executable doc gate per language, fences silently rot through type-API drift across phases.  **R17-DEF-6 status**: reduced to **D.1 only**.  Active branch: `b3d-3d5-format-dsl`.

**Current track:** Phase C ✅ COMPLETE 2026-05-03 on branch `b3d-3d5-format-dsl` — cancellation contract bound across all three bindings; FEATURE_MATRIX `cancellation_contract` × 3 + `lazy_streaming_batch` × 3 rows all flipped.

**Status (2026-05-03): Phase C COMPLETE.**  Four ship commits: `05108cf` (C.0: `docs/architecture/CANCELLATION.md` SSOT — cancel at FFI boundaries; commit-prefix-and-report; behavioral parity by language idiom) → `eef9dcc` (C.3: Go `context.Context` as first parameter on every Client operation method; `sync.Mutex` replaced with a 1-deep `chan struct{}` semaphore so ctx cancellation can abandon the lock wait via `select { case <-lockCh: ...; case <-ctx.Done(): return ctx.Err() }`) → `ef1292d` (C.4: C++ `std::stop_token` as first parameter on every Client operation method; new `ErrorKind::Cancellation` value; pre-FFI guards via `if (stop.stop_requested()) [[unlikely]]` — the `[[unlikely]]` cold-path attribute reclaims ~4% Stream LTL otherwise lost to the new branch) → `c8ac95b` (C.1+C.2: Python `aletheia.asyncio.AletheiaClient` mirrors sync surface method-for-method via `asyncio.to_thread` (17 async methods); new sync `AletheiaClient.send_frames_iter` generator yields `FrameResult` per frame — consumer `break` / `gen.close()` lands the commit-prefix-and-report contract on the sync side; new `SignalOpsMixin(ABC)` extracts `extract_signals` / `update_frame` / `build_frame` to keep `_client.py` under the 1000-line pylint threshold per `feedback_no_weak_config_bumps.md`).  **Cross-binding asymmetry locked in**: `BatchError.partial_results` (Python) carries the committed prefix on **non-cancellation errors only** — sync `send_frames` matches Go `BatchResult.responses` + C++ batch result (full prefix), sync `send_frames_iter` is `[]` (every committed result already yielded), async cancellation propagates `asyncio.CancelledError` verbatim (BaseException since 3.8 → not caught by `except Exception`).  **Bench verification** per `feedback_hot_path_refactor_benchmark.md` (4-batch comparison, 2× C.4 baseline + 2× C.1+C.2): system noise high today (Python lanes drift -14% Stream LTL batch-to-batch with no source changes); averaged Python deltas Stream LTL **-4.7%**, Signal Extraction **-1.6%**, Frame Building **+0.3%**; C++/Go on unchanged `.so` showed comparable inter-batch drift, confirming noise-floor and no mixin-MRO regression.  **Module count unchanged at 237** — no Agda module changes in Phase C; the entire phase is binding-side ergonomics over the existing FFI surface.  See `memory/project_async_api_phase6.md` for design + implementation post-mortem.  Active branch: `b3d-3d5-format-dsl`.

**Status (2026-05-03): Phase B.3 COMPLETE.**  Final ship sequence: B.3.d (`bca99f2`, universal roundtrip theorem) → B.3.e/h/i (`bc7a5fc`, JSON binding + ParsedDBCResponse envelope + C++/Go bindings) → B.3.j (`3673cd2` + `3404dec`, cross-binding corpus parity gate + native canonical wire form) → B.3.f (`019d014`, Python `dbc_to_json` flipped to verified Agda parser; surfaced + fixed two Python-side parity asymmetries: `parse_parsed_dbc_response` now runs `normalize_dbc` so callers see `Fraction` rationals, `FractionJSONEncoder` canonicalised to "emit int when denominator=1") → B.3.g (`2daa2fb`, cantools fallback dropped outright; ~3,560 net LOC removed; `[dbc]` extra retired from `pyproject.toml`).  All three bindings (Python `dbc_to_json` / `parse_dbc_text`, C++ `parse_dbc_text`, Go `ParseDBCText`) now run on the verified Agda parser with B.3.d's universal roundtrip underwriting them; `examples/example.dbc` lost a stray trailing space after `NS_ :` so the strict Agda parser accepts it.  Module count unchanged at **237** post-B.3.e (B.3.f/g/j were Python-side / binding-side / docs only).  LGPL contingency for cantools fully realised.  Lessons captured: `feedback_cross_binding_wire_symmetry.md` (NEW — every wire-shape rule must apply at every binding's serializer AND deserializer; B.3.f/B.3.j surfaced two Python-side asymmetries) + `feedback_heavy_import_handler_split.md` (B.3.e — handler-import-closure heap blowup).  Active branch: `b3d-3d5-format-dsl`.

**Status (2026-05-03): Phase B.3.d COMPLETE.**  Universal target

    ∀ d → WellFormedDBC d → parseText (formatText d) ≡ inj₂ d

proven in `Substrate/Unsafe.agda` (sole consumer of the two stdlib-equivalent bridging axioms; co-located with the axioms by Unsafe-module-policy — see `memory/feedback_unsafe_module_policy.md`).  Layers 1–2 ✅; Layer 3 ✅ (all per-line constructs migrated to Format DSL); Layer 4a ✅ (`70766cd`) + 4b ✅ (`7b17da6`) + 4c-(a) ✅ (`cf091d8`) + 4c-(b) ✅ (`6c7ade3`) + 4c task E ✅ (`bca99f2`, 2026-05-03) — closes B.3.d.  Path A.4 (3d.4 + JSON-mirror, commit `320c5a9` on `b3d-3d4-json-detaint`) lifted the prior 47-module `--without-K`-only cluster to `--safe --without-K` and shipped Path A.5 Bool fast path on `Cache.lookupEntries` / `DBCHelpers.findSignalInList`.

**Layer 3 construct status** (✅ migrated to Format DSL via η-style wrap `parse <leafFmt> >>= many parseNewline >>= …`; % = strict-LOC reduction on production-side):
- BO_ block ✅ — 3d.6 + 3d.7 + 3d.8 (commits `89e04ee` + `42228df` + `f25ca5a`); spine-based `emitMessage-chars-decompose` + 2-stage `pos-eq` patterns established here for future multi-line composers.
- ValueTable ✅ — 3d.5.d (`b374217`, 83%).
- BU_ ✅ — 3d.5.d-BU_ (`abc7d0f`, 73%).
- EV_ ✅ — 3d.5.d-EV_ (`21a3146`, 94%); introduced `discardFmt` (parse-permissive / emit-canonical) + `varTypeFmt` ADT-via-altSum-tree + iso patterns.
- CM_ ✅ — 3d.5.d-CM_ (`7c27100`, 88.6%; heaviest L3 at 2,533L pre-migration); introduced `shift-trail-space` ++ₗ-assoc bridge for `withWSAfter`-baked trailing-space slots; CAN-ID K-elim avoidance via `with buildCANId | buildCANId-rawCanIdℕ ; just .cid | refl = refl`.
- Preamble (VERSION / BS_ / NS_) ✅ — 3d.5.d-3a (`c5b34fb`, 76% combined; Namespace alone 91%); introduced `wsCanonTab` + `nonNewlineRun` DSL constructors; discard-iso pattern over `many nsLineFmt` for fixed-body emit; spec-drift catch via advisor (`withWSCanonOne` vs `withWS` parser permissiveness).
- BA_DEF_ / BA_DEF_REL_ ✅ — 3d.5.d-3c-A (`27aaa8c`, 56% combined / 82% Properties-side); deleted `Properties/Attributes/Type.agda` entirely; introduced `intDecRat` / `natDecRat` DSL constructors; cycle break via `Attributes/Foundations.agda`; 5-case-cong-bridge pattern (vs 25-case enumeration); `concatMap-foldr-bridge` structural induction.
- BA_ / BA_REL_ / BA_DEF_DEF_ ✅ — 3d.5.d-3c-B (`3ab805e`, 4% combined / 29% Properties-side); 7 Properties files refactored (Network/Node/Msg/Sig/EV/Rel/Default; 4044 → 2884 strict-LOC); new modules Format/AttrLine.agda + Format/AttrValue.agda + Properties/Attributes/Assign/Common.agda; specialized `parseAttrAssign-format-roundtrip-RatwNet` for inj₂-deep target position (avoids universal-lemma's L5 EmitsOK obligation that OOMs at -M16G); per-shape numeric dispatchers use **constructor pattern-match + `proj₁`/`proj₂` projections, NOT `with`** (the `with`-abstraction over wide ∃₂ × _⊎_ Σ-types triggers goal-rebuild thrash through inj₂-deep `EmitsOK` reduction even with hoisted helpers — 6+ min OOM → 13s after fix; see `feedback_with_abstraction_traps.md`).  `parseAttrLine` top-level dispatcher unchanged — its 5-way `<|>` chain consumes the migrated sub-parsers transparently.

**Layer 4 progress**:
- 4a ✅ — `Format/SignalGroup.agda` (last per-line construct migrated to DSL) + `Properties/CharClassDisjoint.agda` (4 owed bridge lemmas all discharged).
- 4b ✅ — `Properties/ManyRoundtrip.agda` (polymorphic `many-η-roundtrip` helper) + 5 list-level lifts (SignalGroups / ValueTables / EnvVars / Comments / Messages).
- 4c-(a) ✅ shipped `cf091d8` — parseTopStmt restructured to 7-bucket head-character pattern dispatch + 5 simple section dispatchers + generic `parseTopStmt-on-BA-head` Attribute helper.  TVT pilot validated pivot: 15.7 GB / 273s → 443 MB / 14s (35× memory).  Foundations + BodyBridge + many-η-roundtrip-with-lift extension shipped alongside.  Module count 209 → 218.
- 4c-(b) ✅ shipped `6c7ade3` — TAT universal attribute roundtrip via β prefix-lemma strategy.  3-way DBCAttribute dispatch routes through 31 existing parseAttrLine-roundtrip-* slims; per-shape `emit*-chars-BA-head` Σ-witness lemma + `parseTopStmt-on-BA-head-via-prefix` accept-input-and-prefix-witness lift.  7-case TopStmt scope dispatch 25 min OOM → 1-case via β 13.8s (≥120× speedup).  Module count 218 → 231 (+13).
- **4c task E ✅ shipped `bca99f2` (2026-05-03) — closes Phase B.3.d.**  Tasks B/C/D/E in one commit:
  - **B** `Aggregator/Dispatcher.agda` (TopStmtTypedWF + combined per-section dispatcher) + `Aggregator/ManyTopStmts.agda` (single application of many-η-roundtrip-with-lift); TAT case factors through β prefix witness for nonzero / head-not-newline.
  - **C** `Aggregator/Partition.agda` — partitionTopStmts-bridge: 6 per-section accumulator-style lemmas + 5 foldr-++ + cong-mkCollectedTop with 6 ++ₗ-identityʳ cleanups.
  - **D** `Aggregator/Refine.agda` — collectRawDefs-rawOf passthrough + refineAttributes-on-rawOf inverse.  Lifted `refineAll` from where-block to top-level in `Attributes.agda` so the inductive proof can address `defs ≢ collectRawDefs raws`.
  - **E** `Aggregator/Universal.agda` (`--safe`) — composes WellFormedDBC record + parseDBCText 5-step bind chain + parseTextChars-on-formatChars closure (split into stage 1 cong + stage 2 finalizeParse-on-mkResult-clean to bound rewrite scope; single-shot OOM'd at -M16G).  String-level `parseText-on-formatText` co-located in **`Substrate/Unsafe.agda`** by deliberate Unsafe-module policy (sole axiom consumer; see `memory/feedback_unsafe_module_policy.md`).  Walk-root coverage collapsed from 3 to 1 (`Substrate/Unsafe.agda`).  Module count 231 → 236 (+5, all `--safe --without-K`).

**Format DSL toolkit** (post-3c-A, in `DBC/TextParser/Format.agda`):
- Core constructors: `literal` / `ident` / `nat` / `stringLit` / `pair` / `iso` / `many` / `refined` / `altSum` / `withPrefix`.
- Whitespace family (each with distinct parser permissiveness — see `feedback_format_dsl_ws_family_discipline.md`): `ws` (parser one-or-more, canonical single space) / `wsOpt` (zero-or-more, canonical empty) / `wsCanonOne` (zero-or-more, canonical single space) / `wsCanonTab` (one-or-more, canonical tab) / `withWS` / `withWSOpt` / `withWSCanonOne` / `withWSCanonTab` / `withWSAfter`.
- Refinement carriers: `decRat` / `intDecRat` / `natDecRat`.
- Sugar: `discardFmt` (wire-only fields), `nonNewlineRun` (opaque-tail consumer), `newlineFmt` (LF/CRLF).
- Cycle-break pattern (3d.5.c-ε / 3d.5.d-3c-A): when a Format module would close a cycle, extract cycle-relevant subset to a `Foundations.agda` submodule.

**Path A profile** (post-3d.4 + JSON-mirror, runtime impact retained from `320c5a9`): Stream LTL +12-38% across bindings (Bool fast path); Signal Extraction -2-9% / Frame Building -1-7% (Path A structural cost). All 3d.5+ Format DSL work is proof-only and runtime-neutral on the streaming hot path — the migrated parsers (`parseSignalLine`, `parseValueTable`, `parseMessage`, `parseBU`, `parseEnvVar`, `parseComment`, `parseVersion`, `parseBitTiming`, `parseNamespace`, `parseAttrDef`, `parseAttrDefRel`, `parseRawAttrAssign`, `parseRawAttrRel`, `parseRawAttrDefault`) all run at DBC config time only. Baselines NOT refreshed per user "wait and see" 2026-04-28; COMPILE-pragma escape hatch deferred (requires explicit user approval per `feedback_no_suppression_without_approval`).

**Architectural plan locked 2026-04-26 (post-3d.3b) + amended 2026-04-27 (JSON-mirror addition)** per [PARITY_PLAN.md §B.3.d](docs/development/PARITY_PLAN.md):
1. 3d.4 + JSON-mirror + Path A ✅ shipped 2026-04-28 (`320c5a9`).
2. 3d.5 Format DSL framework: (a) framework core ✅; (b) single-construct validation gate ✅; (c) pinch-point extensions ✅ (α `refined` / β `altSum` + `withPrefix` / γ `CanonicalReceivers` / ε Topology split / ζ `decRat` + `ws`-family / η `parseSignalLine`); **(d) migration of remaining 3a–3d.3 proofs onto DSL ✅ COMPLETE:** ValueTable / BU_ / EV_ / CM_ / Preamble / BA_DEF_ / BA_/BA_REL_/Default. (e) renumbered 3d.6–3d.8 ✅. (f) Layer 4 aggregation: 4a ✅ / 4b ✅ / **4c-(a) ✅** (parseTopStmt head-dispatch + 5 simple dispatchers + Attribute helper) / 4c remaining: TAT universal + many-η-with-lift + partition + refineAttributes inverse + universal aggregator.

**Cross-binding parity roadmap**: [docs/development/PARITY_PLAN.md](docs/development/PARITY_PLAN.md), locked after R17. Active deferrals (R17-DEF-1..6, B.3.d Layer 4 owed lemmas, B.3.d-gated cantools drop) tracked in `memory/project_*.md`.
